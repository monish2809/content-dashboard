import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "i0.wp.com",
      "image.tmdb.org",
      "cdn.socialmedia.com",
      "cdn.arstechnica.net",
      "www.sammyfans.com",
    ],
  },
};

export default nextConfig;
